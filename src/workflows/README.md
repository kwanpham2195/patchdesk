# Flue source discovery

Patchdesk uses Flue's current `src/workflows/` discovery location. No review workflow is defined in the scaffold, so the loopback server exposes no Flue workflow or run route yet.
