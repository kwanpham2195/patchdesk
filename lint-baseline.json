{
  "findings": 0,
  "note": "Repo-wide Oxlint finding count, counted from `npx oxlint --deny-warnings --format=json`'s `diagnostics` array. The repository is at zero: `pnpm lint` blocks in the pull request gates, so this file no longer guards the count. It stays because the ratchet's config gate keys on it — an `.oxlintrc.json` or `tools/oxlint/` change must CHANGE `lint-baseline.json` in the same commit, which a green `pnpm lint` at zero cannot enforce. Staging this file unchanged does not count: it leaves no diff entry. When a config edit moves no count, record the recount here. Recounted at zero on 2026-08-27, after `src/domain/insight-record.ts` joined the parser-boundary override for `anti-slop/no-unknown-parameters`. That file now owns `parseRetainedInsight`, the one parser for a stored Insight's `retained` entry, moved there from `src/adapters/storage/insight-store.ts`, which is already on that list for the same reason. See CONTRIBUTING.md."
}
