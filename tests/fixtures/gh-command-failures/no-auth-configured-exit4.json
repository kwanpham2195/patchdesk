{
  "capturedWith": "gh 2.96.0",
  "capturedAt": "2026-08-18",
  "command": "GH_CONFIG_DIR=<empty dir> gh api user",
  "synthetic": false,
  "exitCode": 4,
  "stdout": "",
  "stderr": "To get started with GitHub CLI, please run:  gh auth login\nAlternatively, populate the GH_TOKEN environment variable with a GitHub API authentication token.\n",
  "expectedTag": "CommandAuthenticationRequired",
  "note": "Exit code 4 is gh's documented 'command requires authentication' code, but this fixture exercises the regex fallback specifically: the message contains the literal phrase 'gh auth login', which isAuthenticationFailure already matches. Plan 007 deliberately does not add a separate exit-code-4 shortcut (see execution report); this fixture proves the existing phrase-based coverage already handles the no-credentials case."
}
