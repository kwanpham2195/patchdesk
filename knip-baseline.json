{
  "issues": 0,
  "note": "Repo-wide Knip issue count, counted from `pnpm exec knip --reporter json --no-exit-code` by adding up every issue array in every `issues` entry. The count is zero, so scripts/check-knip-ratchet.mjs (pnpm knip:ratchet) now blocks every new unused file, export, and type: `pnpm knip` and the ratchet are the same gate at this number. Keep it at zero — the fix for a new finding is to delete the dead code or drop the `export` keyword, not to raise this number. See CONTRIBUTING.md."
}
