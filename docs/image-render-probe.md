# Image render probe

Throwaway branch. Exists only to open a pull request whose description carries
images, so the Conversation view can be checked against a non-terminal review.
Close the pull request and delete this branch once the check is done.
