deslop	unused-dependency	package.json	Unused dependency: `@fontsource-variable/geist`
deslop	unused-dev-dependency	package.json	Unused devDependency: `@electron-toolkit/utils`
deslop	unused-export	src/adapters/storage/json-file.ts	Unused export: `appendJsonLine` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.
deslop	unused-export	src/domain/contracts.ts	Unused export: `parseGitHubPullRequestDto` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.
deslop	unused-export	src/domain/contracts.ts	Unused export: `parseReviewPrWorkflowOutput` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.
deslop	unused-export	src/domain/contracts.ts	Unused export: `parseStartReviewRequest` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.
deslop	unused-export	src/domain/contracts.ts	Unused export: `reviewPrWorkflowOutputSchema` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.
deslop	unused-export	src/domain/insight-provider.ts	Unused export: `isInsightProvider` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.
deslop	unused-export	src/domain/insight-provider.ts	Unused export: `parseInsightSelection` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.
deslop	unused-export	src/domain/log-entry.ts	Unused export: `fitsLogEntryBytes` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.
deslop	unused-export	src/domain/pending-review.ts	Unused export: `isPendingReviewConfirmed` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.
deslop	unused-export	src/domain/pull-request.ts	Unused export: `pullRequestInputSchema` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.
deslop	unused-export	src/renderer/src/components/pr-overview-sheet.tsx	Unused export: `PullRequestOverviewSheet` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.
deslop	unused-export	src/renderer/src/components/review-workbench.tsx	Unused export: `usePublishedFeedbackNavigation` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.
deslop	unused-export	src/renderer/src/flows/inbox-flow.tsx	Unused export: `InboxScreen` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.
deslop	unused-export	src/renderer/src/flows/inbox-flow.tsx	Unused export: `Pending` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.
deslop	unused-export	src/renderer/src/review-copy.ts	Unused export: `WALKTHROUGH_LIFECYCLE_KEYS` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.
deslop	unused-export	src/services/review-commit-service.ts	Unused export: `commitDiffFailureReason` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.
deslop	unused-export	src/services/review-preparation-journal.ts	Unused export: `promoteStagedArtifact` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.
deslop	unused-export	src/services/walkthrough-operation.ts	Unused export: `walkthroughInputSchema` is exported but no module imports it, so it expands the public surface and can mislead callers about supported API.
deslop	unused-file	src/renderer/src/components/dashboard-empty-state.tsx	Unused file is not reachable from any entry point, so it adds maintenance surface without shipping any code.
deslop	unused-file	src/renderer/src/components/ui/dropdown-menu.tsx	Unused file is not reachable from any entry point, so it adds maintenance surface without shipping any code.
deslop	unused-file	src/renderer/src/components/ui/item.tsx	Unused file is not reachable from any entry point, so it adds maintenance surface without shipping any code.
deslop	unused-file	src/renderer/src/components/ui/toggle-group.tsx	Unused file is not reachable from any entry point, so it adds maintenance surface without shipping any code.
deslop	unused-file	src/renderer/src/components/ui/toggle.tsx	Unused file is not reachable from any entry point, so it adds maintenance surface without shipping any code.
deslop	unused-file	src/renderer/src/format-byte-size.ts	Unused file is not reachable from any entry point, so it adds maintenance surface without shipping any code.
deslop	unused-file	src/renderer/src/hooks/use-mobile.ts	Unused file is not reachable from any entry point, so it adds maintenance surface without shipping any code.
deslop	unused-file	src/renderer/src/main.tsx	Unused file is not reachable from any entry point, so it adds maintenance surface without shipping any code.
deslop	unused-file	src/renderer/src/review-identity.ts	Unused file is not reachable from any entry point, so it adds maintenance surface without shipping any code.
react	only-export-components	src/renderer/src/components/ui/badge.tsx	Fast refresh only works when a file only exports components. Use a new file to share constants or functions between components.
react	only-export-components	src/renderer/src/components/ui/button-group.tsx	Fast refresh only works when a file only exports components. Use a new file to share constants or functions between components.
react	only-export-components	src/renderer/src/components/ui/button.tsx	Fast refresh only works when a file only exports components. Use a new file to share constants or functions between components.
react	only-export-components	src/renderer/src/components/ui/tabs.tsx	Fast refresh only works when a file only exports components. Use a new file to share constants or functions between components.
react	only-export-components	src/renderer/src/components/ui/toggle.tsx	Fast refresh only works when a file only exports components. Use a new file to share constants or functions between components.
react-doctor	async-await-in-loop	src/adapters/github/workspace-origin-finder.ts	This makes the for…of loop slow because each await runs one after another, so collect the independent calls & run them together with `await Promise.all(items.map(...))`
react-doctor	async-await-in-loop	src/adapters/github/workspace-origin-finder.ts	This makes the for…of loop slow because each await runs one after another, so collect the independent calls & run them together with `await Promise.all(items.map(...))`
react-doctor	async-await-in-loop	src/adapters/storage/review-artifact-storage.ts	This makes the for…of loop slow because each await runs one after another, so collect the independent calls & run them together with `await Promise.all(items.map(...))`
react-doctor	async-await-in-loop	src/adapters/storage/review-artifact-storage.ts	This makes the for…of loop slow because each await runs one after another, so collect the independent calls & run them together with `await Promise.all(items.map(...))`
react-doctor	async-await-in-loop	src/adapters/storage/review-artifact-storage.ts	This makes the for…of loop slow because each await runs one after another, so collect the independent calls & run them together with `await Promise.all(items.map(...))`
react-doctor	async-await-in-loop	src/adapters/storage/review-observation-journal-store.ts	This makes the for…of loop slow because each await runs one after another, so collect the independent calls & run them together with `await Promise.all(items.map(...))`
react-doctor	async-await-in-loop	src/adapters/storage/review-session-store.ts	This makes the for…of loop slow because each await runs one after another, so collect the independent calls & run them together with `await Promise.all(items.map(...))`
react-doctor	async-await-in-loop	src/services/dashboard-service.ts	This makes the for…of loop slow because each await runs one after another, so collect the independent calls & run them together with `await Promise.all(items.map(...))`
react-doctor	async-await-in-loop	src/services/insight-run-coordinator.ts	This makes the for…of loop slow because each await runs one after another, so collect the independent calls & run them together with `await Promise.all(items.map(...))`
react-doctor	async-await-in-loop	src/services/insight-run-coordinator.ts	This makes the for…of loop slow because each await runs one after another, so collect the independent calls & run them together with `await Promise.all(items.map(...))`
react-doctor	async-await-in-loop	src/services/review-inspector.ts	This makes the for…of loop slow because each await runs one after another, so collect the independent calls & run them together with `await Promise.all(items.map(...))`
react-doctor	async-await-in-loop	src/services/review-preparation-journal.ts	This makes the for…of loop slow because each await runs one after another, so collect the independent calls & run them together with `await Promise.all(items.map(...))`
react-doctor	async-await-in-loop	src/services/review-preparation-journal.ts	This makes the for…of loop slow because each await runs one after another, so collect the independent calls & run them together with `await Promise.all(items.map(...))`
react-doctor	async-await-in-loop	src/services/review-preparation-journal.ts	This makes the for…of loop slow because each await runs one after another, so collect the independent calls & run them together with `await Promise.all(items.map(...))`
react-doctor	async-await-in-loop	src/services/review-preparation-journal.ts	This makes the for…of loop slow because each await runs one after another, so collect the independent calls & run them together with `await Promise.all(items.map(...))`
react-doctor	async-await-in-loop	src/services/review-recovery-service.ts	This makes the for…of loop slow because each await runs one after another, so collect the independent calls & run them together with `await Promise.all(items.map(...))`
react-doctor	async-await-in-loop	src/services/review-recovery-service.ts	This makes the for…of loop slow because each await runs one after another, so collect the independent calls & run them together with `await Promise.all(items.map(...))`
react-doctor	async-await-in-loop	src/services/review-recovery-service.ts	This makes the for…of loop slow because each await runs one after another, so collect the independent calls & run them together with `await Promise.all(items.map(...))`
react-doctor	exhaustive-deps	src/renderer/src/app.tsx	`useEffect` can run with a stale `dashboard` & show your users old data.
react-doctor	exhaustive-deps	src/renderer/src/components/diff-workbench.tsx	`useEffect` can run with a stale `updatePreferences` & show your users old data.
react-doctor	exhaustive-deps	src/renderer/src/components/logs-panel.tsx	`useEffect` can run with a stale `pausedRef, afterSeqRef` & show your users old data.
react-doctor	exhaustive-deps	src/renderer/src/components/maintainer-inbox.tsx	`useEffect` can run with a stale `selectView` & show your users old data.
react-doctor	exhaustive-deps	src/renderer/src/components/settings-modal.tsx	`useEffect` can run with a stale `initialSection` & show your users old data.
react-doctor	exhaustive-deps	src/renderer/src/components/watchlist-panel.tsx	`useEffect` can run with a stale `discover` & show your users old data.
react-doctor	exhaustive-deps	src/renderer/src/components/watchlist-panel.tsx	`workspaceRoots` is rebuilt every render, so `useMemo` runs every time.
react-doctor	exhaustive-deps	src/renderer/src/flows/inbox-flow.tsx	`useEffect` can run with a stale `dashboard, openStoredReviewById` & show your users old data.
react-doctor	exhaustive-deps	src/renderer/src/flows/review-workbench-flow.tsx	`useCallback` can run with a stale `onWorkbenchPatch` & show your users old data.
react-doctor	exhaustive-deps	src/renderer/src/flows/review-workbench-flow.tsx	`useCallback` can run with a stale `workbenchRef, onWorkbenchPatchRef` & show your users old data.
react-doctor	exhaustive-deps	src/renderer/src/flows/review-workbench-flow.tsx	`useCallback` can run with a stale `workbenchRef, recentWritesRef, replaceWorkbench, onWorkbenchPatchRef, detectedStaleFreshnessRef` & show your users old data.
react-doctor	exhaustive-deps	src/renderer/src/flows/review-workbench-flow.tsx	`useCallback` can run with a stale `workbenchRef, refreshingRef` & show your users old data.
react-doctor	exhaustive-deps	src/renderer/src/flows/review-workbench-flow.tsx	`useCallback` can run with a stale `workbenchRef` & show your users old data.
react-doctor	exhaustive-deps	src/renderer/src/hooks/use-commit-diff.ts	`useEffect` can run with a stale `loader` & show your users old data.
react-doctor	exhaustive-deps	src/renderer/src/hooks/use-insight-run.ts	`useEffect` can run with a stale `onInsightPatchRef, onWorkbenchReplaceRef, onCompletedRef` & show your users old data.
react-doctor	js-cache-property-access	src/adapters/storage/merge-operation-store.ts	This slows the loop because operation.value.state._tag is read 3 times inside it, so read it once into a variable at the top
react-doctor	js-combine-iterations	src/adapters/pi/pi-runtime-model-catalog.ts	This loops over your list twice because .filter().map() makes two passes, so do it in one pass with .reduce() or a for...of loop
react-doctor	js-combine-iterations	src/adapters/pi/pi-runtime-model-catalog.ts	This loops over your list twice because .flatMap().map() makes two passes, so do it in one pass with .reduce() or a for...of loop
react-doctor	js-combine-iterations	src/domain/diff-anchor.ts	This loops over your list twice because .filter().map() makes two passes, so do it in one pass with .reduce() or a for...of loop
react-doctor	js-combine-iterations	src/domain/diff-anchor.ts	This loops over your list twice because .filter().map() makes two passes, so do it in one pass with .reduce() or a for...of loop
react-doctor	js-combine-iterations	src/main/external-navigation.ts	This loops over your list twice because .map().filter() makes two passes, so do it in one pass with .reduce() or a for...of loop
react-doctor	js-combine-iterations	src/renderer/src/components/narrative-walkthrough-diff.tsx	This loops over your list twice because .map().filter() makes two passes, so do it in one pass with .reduce() or a for...of loop
react-doctor	js-combine-iterations	src/renderer/src/components/review-diff-view.tsx	This loops over your list twice because .filter().map() makes two passes, so do it in one pass with .reduce() or a for...of loop
react-doctor	js-combine-iterations	src/renderer/src/components/review-diff-view.tsx	This loops over your list twice because .filter().map() makes two passes, so do it in one pass with .reduce() or a for...of loop
react-doctor	js-combine-iterations	src/renderer/src/components/review-diff-view.tsx	This loops over your list twice because .filter().map() makes two passes, so do it in one pass with .reduce() or a for...of loop
react-doctor	js-combine-iterations	src/renderer/src/components/review-diff-view.tsx	This loops over your list twice because .filter().map() makes two passes, so do it in one pass with .reduce() or a for...of loop
react-doctor	js-combine-iterations	src/renderer/src/flows/inbox-flow.tsx	This loops over your list twice because .filter().map() makes two passes, so do it in one pass with .reduce() or a for...of loop
react-doctor	js-combine-iterations	src/renderer/src/flows/settings-flow.tsx	This loops over your list twice because .filter().map() makes two passes, so do it in one pass with .reduce() or a for...of loop
react-doctor	js-combine-iterations	src/services/analysis-review-body.ts	This loops over your list twice because .filter().map() makes two passes, so do it in one pass with .reduce() or a for...of loop
react-doctor	js-combine-iterations	src/services/review-refresh-service.ts	This loops over your list twice because .map().filter() makes two passes, so do it in one pass with .reduce() or a for...of loop
react-doctor	js-hoist-intl	src/renderer/src/components/review-navigator.tsx	This is slow because new Intl.RelativeTimeFormat() rebuilds on every call inside a function, so move it to the top of the file, or wrap it in useMemo
react-doctor	js-hoist-intl	src/renderer/src/components/review-workbench.tsx	This is slow because new Intl.RelativeTimeFormat() rebuilds on every call inside a function, so move it to the top of the file, or wrap it in useMemo
react-doctor	js-hoist-intl	src/renderer/src/flows/review-workbench-flow.tsx	This is slow because new Intl.DateTimeFormat() rebuilds on every call inside a function, so move it to the top of the file, or wrap it in useMemo
react-doctor	js-set-map-lookups	src/renderer/src/flows/review-workbench-flow.tsx	This scales poorly because `array.includes()` inside a loop scans the whole list every time. Use a Set for constant-time lookups.
react-doctor	jsx-no-constructed-context-values	src/renderer/src/components/ui/toggle-group.tsx	Every reader of this context redraws on each render because you build its `value` inline.
react-doctor	no-adjust-state-on-prop-change	src/renderer/src/components/diff-workbench.tsx	This effect adjusts state after a prop changes, so users briefly see the stale value.
react-doctor	no-adjust-state-on-prop-change	src/renderer/src/components/finish-review-dialog.tsx	This effect adjusts state after a prop changes, so users briefly see the stale value.
react-doctor	no-adjust-state-on-prop-change	src/renderer/src/components/finish-review-dialog.tsx	This effect adjusts state after a prop changes, so users briefly see the stale value.
react-doctor	no-adjust-state-on-prop-change	src/renderer/src/components/finish-review-dialog.tsx	This effect adjusts state after a prop changes, so users briefly see the stale value.
react-doctor	no-adjust-state-on-prop-change	src/renderer/src/components/finish-review-dialog.tsx	This effect adjusts state after a prop changes, so users briefly see the stale value.
react-doctor	no-adjust-state-on-prop-change	src/renderer/src/components/markdown-lightbox.tsx	This effect adjusts state after a prop changes, so users briefly see the stale value.
react-doctor	no-adjust-state-on-prop-change	src/renderer/src/components/markdown-lightbox.tsx	This effect adjusts state after a prop changes, so users briefly see the stale value.
react-doctor	no-adjust-state-on-prop-change	src/renderer/src/components/review-workbench.tsx	This effect adjusts state after a prop changes, so users briefly see the stale value.
react-doctor	no-adjust-state-on-prop-change	src/renderer/src/components/review-workbench.tsx	This effect adjusts state after a prop changes, so users briefly see the stale value.
react-doctor	no-adjust-state-on-prop-change	src/renderer/src/components/review-workbench.tsx	This effect adjusts state after a prop changes, so users briefly see the stale value.
react-doctor	no-adjust-state-on-prop-change	src/renderer/src/components/review-workbench.tsx	This effect adjusts state after a prop changes, so users briefly see the stale value.
react-doctor	no-adjust-state-on-prop-change	src/renderer/src/components/review-workbench.tsx	This effect adjusts state after a prop changes, so users briefly see the stale value.
react-doctor	no-adjust-state-on-prop-change	src/renderer/src/components/summary-review-dialog.tsx	This effect adjusts state after a prop changes, so users briefly see the stale value.
react-doctor	no-adjust-state-on-prop-change	src/renderer/src/components/summary-review-dialog.tsx	This effect adjusts state after a prop changes, so users briefly see the stale value.
react-doctor	no-adjust-state-on-prop-change	src/renderer/src/components/summary-review-dialog.tsx	This effect adjusts state after a prop changes, so users briefly see the stale value.
react-doctor	no-adjust-state-on-prop-change	src/renderer/src/components/summary-review-dialog.tsx	This effect adjusts state after a prop changes, so users briefly see the stale value.
react-doctor	no-adjust-state-on-prop-change	src/renderer/src/components/summary-review-dialog.tsx	This effect adjusts state after a prop changes, so users briefly see the stale value.
react-doctor	no-adjust-state-on-prop-change	src/renderer/src/components/summary-review-dialog.tsx	This effect adjusts state after a prop changes, so users briefly see the stale value.
react-doctor	no-adjust-state-on-prop-change	src/renderer/src/components/summary-review-dialog.tsx	This effect adjusts state after a prop changes, so users briefly see the stale value.
react-doctor	no-adjust-state-on-prop-change	src/renderer/src/flows/review-workbench-flow.tsx	This effect adjusts state after a prop changes, so users briefly see the stale value.
react-doctor	no-adjust-state-on-prop-change	src/renderer/src/flows/settings-flow.tsx	This effect adjusts state after a prop changes, so users briefly see the stale value.
react-doctor	no-adjust-state-on-prop-change	src/renderer/src/hooks/use-insight-run.ts	This effect adjusts state after a prop changes, so users briefly see the stale value.
react-doctor	no-adjust-state-on-prop-change	src/renderer/src/hooks/use-insight-run.ts	This effect adjusts state after a prop changes, so users briefly see the stale value.
react-doctor	no-adjust-state-on-prop-change	src/renderer/src/hooks/use-insight-run.ts	This effect adjusts state after a prop changes, so users briefly see the stale value.
react-doctor	no-adjust-state-on-prop-change	src/renderer/src/hooks/use-review-diff-hydration.ts	This effect adjusts state after a prop changes, so users briefly see the stale value.
react-doctor	no-array-index-as-key	src/renderer/src/components/generated-markdown.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "key".
react-doctor	no-array-index-as-key	src/renderer/src/components/generated-markdown.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "key".
react-doctor	no-array-index-as-key	src/renderer/src/components/generated-markdown.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "key".
react-doctor	no-array-index-as-key	src/renderer/src/components/generated-markdown.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "key".
react-doctor	no-array-index-as-key	src/renderer/src/components/generated-markdown.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "key".
react-doctor	no-array-index-as-key	src/renderer/src/components/generated-markdown.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "key".
react-doctor	no-array-index-as-key	src/renderer/src/components/generated-markdown.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "key".
react-doctor	no-array-index-as-key	src/renderer/src/components/generated-markdown.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "key".
react-doctor	no-array-index-as-key	src/renderer/src/components/generated-markdown.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "key".
react-doctor	no-array-index-as-key	src/renderer/src/components/generated-markdown.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "key".
react-doctor	no-array-index-as-key	src/renderer/src/components/generated-markdown.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "key".
react-doctor	no-array-index-as-key	src/renderer/src/components/generated-markdown.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "key".
react-doctor	no-array-index-as-key	src/renderer/src/components/generated-markdown.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "key".
react-doctor	no-array-index-as-key	src/renderer/src/components/generated-markdown.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "key".
react-doctor	no-array-index-as-key	src/renderer/src/components/generated-markdown.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "key".
react-doctor	no-array-index-as-key	src/renderer/src/components/generated-markdown.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "key".
react-doctor	no-array-index-as-key	src/renderer/src/components/pull-request-description.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "key".
react-doctor	no-array-index-as-key	src/renderer/src/components/pull-request-description.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "key".
react-doctor	no-array-index-as-key	src/renderer/src/components/pull-request-description.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "key".
react-doctor	no-array-index-as-key	src/renderer/src/components/pull-request-description.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "key".
react-doctor	no-array-index-as-key	src/renderer/src/components/pull-request-description.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "key".
react-doctor	no-array-index-as-key	src/renderer/src/components/pull-request-description.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "key".
react-doctor	no-array-index-as-key	src/renderer/src/components/pull-request-description.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "key".
react-doctor	no-array-index-as-key	src/renderer/src/components/pull-request-description.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "key".
react-doctor	no-array-index-as-key	src/renderer/src/components/pull-request-description.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "key".
react-doctor	no-array-index-as-key	src/renderer/src/components/pull-request-description.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "key".
react-doctor	no-array-index-as-key	src/renderer/src/components/pull-request-description.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "key".
react-doctor	no-array-index-as-key	src/renderer/src/components/pull-request-description.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "key".
react-doctor	no-array-index-as-key	src/renderer/src/components/pull-request-description.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "key".
react-doctor	no-array-index-as-key	src/renderer/src/components/pull-request-description.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "key".
react-doctor	no-array-index-as-key	src/renderer/src/components/pull-request-description.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "key".
react-doctor	no-array-index-as-key	src/renderer/src/components/pull-request-description.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "key".
react-doctor	no-array-index-as-key	src/renderer/src/components/pull-request-description.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "key".
react-doctor	no-array-index-as-key	src/renderer/src/components/pull-request-description.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "key".
react-doctor	no-array-index-as-key	src/renderer/src/components/pull-request-description.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "key".
react-doctor	no-array-index-as-key	src/renderer/src/components/pull-request-description.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "key".
react-doctor	no-array-index-as-key	src/renderer/src/components/review-diff-view.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "index".
react-doctor	no-array-index-as-key	src/renderer/src/components/ui/field.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "index".
react-doctor	no-array-index-as-key	src/renderer/src/flows/settings-flow.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "index".
react-doctor	no-array-index-as-key	src/renderer/src/flows/settings-flow.tsx	Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like `key={item.id}`, not the array index "index".
react-doctor	no-children-prop	src/renderer/src/use-lightbox.ts	A `children` prop can override or hide nested children, so the component may render different content than the JSX shows.
react-doctor	no-derived-useState	src/renderer/src/flows/review-workbench-flow.tsx	Your users see a stale value when prop "workbench" changes because useState copies it once.
react-doctor	no-effect-chain	src/renderer/src/components/maintainer-inbox.tsx	Your screen redraws several times from a single action because one useEffect changes "selectedKey", which sets off this one.
react-doctor	no-fetch-response-used-without-status-check	src/main/desktop-bridge.ts	`fetch()` resolves (does not reject) on HTTP 4xx/5xx, so this unchecked body read may treat an HTTP error payload like a successful response. Check `response.ok`/`response.status`, or deliberately handle the API's error payload, before reading the body.
react-doctor	no-giant-component	src/renderer/src/app.tsx	Component "App" is over 300 lines long, which is hard to read & change. Split it into a few smaller components.
react-doctor	no-giant-component	src/renderer/src/components/maintainer-inbox.tsx	Component "MaintainerInbox" is over 300 lines long, which is hard to read & change. Split it into a few smaller components.
react-doctor	no-giant-component	src/renderer/src/components/narrative-walkthrough.tsx	Component "NarrativeWalkthrough" is over 300 lines long, which is hard to read & change. Split it into a few smaller components.
react-doctor	no-giant-component	src/renderer/src/components/review-diff-view.tsx	Component "ReviewDiffSurface" is over 300 lines long, which is hard to read & change. Split it into a few smaller components.
react-doctor	no-giant-component	src/renderer/src/components/review-workbench.tsx	Component "ReviewWorkbench" is over 300 lines long, which is hard to read & change. Split it into a few smaller components.
react-doctor	no-giant-component	src/renderer/src/flows/review-workbench-flow.tsx	Component "InsightsSlot" is over 300 lines long, which is hard to read & change. Split it into a few smaller components.
react-doctor	no-giant-component	src/renderer/src/flows/review-workbench-flow.tsx	Component "ReviewWorkbenchFlow" is over 300 lines long, which is hard to read & change. Split it into a few smaller components.
react-doctor	no-giant-component	src/renderer/src/flows/settings-flow.tsx	Component "SettingsFlow" is over 300 lines long, which is hard to read & change. Split it into a few smaller components.
react-doctor	no-json-parse-stringify-clone	tests/domain/log-entry.test.ts	`JSON.parse(JSON.stringify(x))` deep-clones by re-serializing: it is slow on large objects and silently drops `undefined`, functions, `Date`/`Map`/`Set`, and cyclic references. Use `structuredClone(x)`.
react-doctor	no-loading-flag-reset-outside-finally	src/renderer/src/app.tsx	This resets a loading/busy flag only on the success path: if the awaited call rejects the reset never runs and the flag stays stuck truthy (a spinner that never stops, a button disabled forever). Move the reset into a `finally` block, or mirror it on every catch, so it clears on rejection too.
react-doctor	no-loading-flag-reset-outside-finally	src/renderer/src/components/settings-modal.tsx	This resets a loading/busy flag only on the success path: if the awaited call rejects the reset never runs and the flag stays stuck truthy (a spinner that never stops, a button disabled forever). Move the reset into a `finally` block, or mirror it on every catch, so it clears on rejection too.
react-doctor	no-pass-data-to-parent	src/renderer/src/flows/settings-flow.tsx	Handing data back to a parent from a useEffect costs your users an extra render.
react-doctor	no-pass-live-state-to-parent	src/renderer/src/components/review-workbench.tsx	Pushing state up to a parent from a useEffect costs your users an extra render.
react-doctor	no-pass-live-state-to-parent	src/renderer/src/flows/review-workbench-flow.tsx	Pushing state up to a parent from a useEffect costs your users an extra render.
react-doctor	no-pass-live-state-to-parent	src/renderer/src/flows/review-workbench-flow.tsx	Pushing state up to a parent from a useEffect costs your users an extra render.
react-doctor	no-pass-live-state-to-parent	src/renderer/src/flows/settings-flow.tsx	Pushing state up to a parent from a useEffect costs your users an extra render.
react-doctor	no-prop-callback-in-effect	src/renderer/src/components/review-workbench.tsx	Your parent re-renders on every local state change because this useEffect calls the prop "onStateChange" just to stay in sync.
react-doctor	no-prop-callback-in-effect	src/renderer/src/flows/settings-flow.tsx	Your parent re-renders on every local state change because this useEffect calls the prop "onDirtyChange" just to stay in sync.
react-doctor	no-reset-all-state-on-prop-change	src/renderer/src/components/markdown-lightbox.tsx	Your users briefly see stale state when a prop changes because this useEffect clears all state.
react-doctor	no-reset-all-state-on-prop-change	src/renderer/src/components/summary-review-dialog.tsx	Your users briefly see stale state when a prop changes because this useEffect clears all state.
react-doctor	no-set-state-after-await-in-effect	src/renderer/src/flows/inbox-flow.tsx	This setter runs after `await`, so overlapping re-runs of the effect can resolve out of order and write stale state; gate it behind a cancellation/ignore flag or return a cleanup that cancels the work.
react-doctor	no-set-state-after-await-in-effect	src/renderer/src/hooks/use-insight-run.ts	This setter runs after `await`, so overlapping re-runs of the effect can resolve out of order and write stale state; gate it behind a cancellation/ignore flag or return a cleanup that cancels the work.
react-doctor	no-side-effect-in-state-updater-function	src/renderer/src/components/review-workbench.tsx	This side-effecting call runs inside a state updater, which React may invoke more than once. Move it outside the setter after computing the next state.
react-doctor	no-static-element-interactions	src/renderer/src/components/narrative-walkthrough.tsx	Screen reader users can't tell this click handler is interactive because it has no `role`, so add a `role` or use a button or link.
react-doctor	no-unguarded-throwing-parse-call	src/main/electron-main.ts	This builds a `URL` from a runtime URL/route value (`params`, `searchParams`, a `location` field), which throws `TypeError` on a malformed string and crashes render. Guard it with `URL.canParse`, pass a base-URL second argument, or wrap the call in a try/catch.
react-doctor	no-unowned-async-error-clear	src/renderer/src/flows/settings-flow.tsx	An older async response can clear error state owned by a newer request. Check that this request still owns the error before clearing it, keep ownership in the state update, or key the state owner by request ID.
react-doctor	prefer-html-dialog	src/renderer/src/components/markdown-lightbox.tsx	Screen reader users get native dialog semantics for free from the HTML `<dialog>` element, so swap this `role="dialog"` element for `<dialog>` and open it with `dialog.show()` (non-modal) or `dialog.showModal()` (modal).
react-doctor	prefer-module-scope-static-value	src/renderer/src/components/finding-evidence-hunk.tsx	`preferences` inside `FindingEvidenceHunk` uses no local state but is rebuilt every render, so it looks new each time & breaks memoized children. Move it to the top of the file, outside the component.
react-doctor	prefer-module-scope-static-value	src/renderer/src/flows/app-fixtures.tsx	`walkthrough` inside `WalkthroughFixture` uses no local state but is rebuilt every render, so it looks new each time & breaks memoized children. Move it to the top of the file, outside the component.
react-doctor	prefer-tag-over-role	src/renderer/src/components/ui/item.tsx	Screen reader users get more reliable semantics from `<ul>` than `role="list"`, so use `<ul>` instead.
react-doctor	prefer-useReducer	src/renderer/src/app.tsx	"App" updates 6 separate useState values in one place — state that changes together is easier to keep consistent as a single useReducer action.
react-doctor	prefer-useReducer	src/renderer/src/components/finish-review-dialog.tsx	"FinishReviewDialog" updates 5 separate useState values in one place — state that changes together is easier to keep consistent as a single useReducer action.
react-doctor	prefer-useReducer	src/renderer/src/components/maintainer-inbox.tsx	"MaintainerInbox" updates 8 separate useState values in one place — state that changes together is easier to keep consistent as a single useReducer action.
react-doctor	prefer-useReducer	src/renderer/src/components/review-workbench.tsx	"ReviewWorkbench" updates 5 separate useState values in one place — state that changes together is easier to keep consistent as a single useReducer action.
react-doctor	prefer-useReducer	src/renderer/src/components/summary-review-dialog.tsx	"SummaryReviewDialog" updates 6 separate useState values in one place — state that changes together is easier to keep consistent as a single useReducer action.
react-doctor	prefer-useReducer	src/renderer/src/flows/review-workbench-flow.tsx	"InsightsSlot" updates 6 separate useState values in one place — state that changes together is easier to keep consistent as a single useReducer action.
react-doctor	require-pnpm-hardening	pnpm-workspace.yaml	pnpm-workspace.yaml is missing `minimumReleaseAge` — newly published versions can ship malware that gets caught and unpublished within hours
react-doctor	require-pnpm-hardening	pnpm-workspace.yaml	pnpm-workspace.yaml is missing `trustPolicy` — without `no-downgrade`, pnpm silently accepts packages whose trust signals (provenance, signatures) weaken between updates
react-doctor	rerender-lazy-ref-init	src/renderer/src/components/app-shell.tsx	useRef(destinationKey()) rebuilds this value on every render & throws it away.
react-doctor	rerender-lazy-ref-init	src/renderer/src/flows/review-workbench-flow.tsx	useRef(snapshotKey()) rebuilds this value on every render & throws it away.
react-doctor	rerender-memo-with-default-value	src/renderer/src/components/narrative-walkthrough-diff.tsx	This reruns hooks that list it in their dependency array because default prop value [] makes a brand new array every render, so move it to a constant at the top of the file
react-doctor	rerender-memo-with-default-value	src/renderer/src/components/review-diff-view.tsx	This reruns hooks that list it in their dependency array because default prop value [] makes a brand new array every render, so move it to a constant at the top of the file
react-doctor	rerender-state-only-in-handlers	src/renderer/src/components/settings-modal.tsx	Each update to "dirty" redraws your component for nothing because this useState is set but never shown on screen.
react-doctor	rerender-state-only-in-handlers	src/renderer/src/flows/review-workbench-flow.tsx	Each update to "preferences" redraws your component for nothing because this useState is set but never shown on screen.
react-doctor	server-sequential-independent-await	src/services/codex-insight-invoker.ts	This await doesn't use the previous result, so your users wait twice as long for nothing.
react-doctor	server-sequential-independent-await	src/services/insight-provider-catalog.ts	This await doesn't use the previous result, so your users wait twice as long for nothing.
react-doctor	server-sequential-independent-await	src/services/review-observation-service.ts	This await doesn't use the previous result, so your users wait twice as long for nothing.
react-doctor	server-sequential-independent-await	src/services/review-preparation-journal.ts	This await doesn't use the previous result, so your users wait twice as long for nothing.
react-doctor	server-sequential-independent-await	src/services/review-recovery-service.ts	This await doesn't use the previous result, so your users wait twice as long for nothing.
react-doctor	server-sequential-independent-await	src/services/review-workbench-projection.ts	This await doesn't use the previous result, so your users wait twice as long for nothing.
react-doctor	server-sequential-independent-await	src/services/review-workbench-projection.ts	This await doesn't use the previous result, so your users wait twice as long for nothing.
